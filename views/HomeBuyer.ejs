<%- include('partials/headbuyer', {title: 'Shop'}) %>

<a href="/admin/product/add"><button>Add New Product</button></a>
<br>
<br>
<table>
  <% products.forEach(p => { %>
    <tr>
      <td>
        <a href="/products/<%= p.id %>">
          <img src="<%= p.image %>" alt="<%= p.name %>" width="150px">
        </a>
      </td>
      <td><%= p.name %></td>
      <td><%= p.priceUSD %></td>
    </tr>
  <% }) %>
</table>

<%- include('partials/footer') %>